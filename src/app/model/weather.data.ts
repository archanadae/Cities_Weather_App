import { Weather } from '../model/weather';

export const WEATHER_ITEMS: Weather[] = [
    new Weather('Chicago', 'SNOW', -10),
    new Weather('New York', 'WINDY', 5),
    new Weather('Arizona', 'SUNNY', 15),
    new Weather('Ohio', 'RAINY', 20),
];